write code for substraction of two number
solution:-
  #include<stdio.h>
  int main()
  {
    int p=40;
    int q=20;
    int sub=0;
    sub=p-q;
    printf("p value %d/n",p);
    printf("q value %d/n",q);
    printf("sub value %d/n",sub);

   printf("p address=%d/n",&p);
   printf("q address=%d/n",&q);
   printf("sub address=%d/n",sub);
   return 0;
}
